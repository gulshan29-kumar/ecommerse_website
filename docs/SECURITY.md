# Security Policy

- **Input Sanitization**: All user inputs in address creation, search queries, and coupon codes are sanitized against XSS attacks.
- **Access Control**: Role-based routing separating customer storefront, vendor store manager, and platform administration.
